import React, { FC } from 'react'
import Header from '../Header';
import Footer from '../Footer';

interface pageProps {
  
}

const page: FC<pageProps> = ({  }) => {
  return (
    <>
      <Header />
      <Footer />
    </>
  )
}

export default page;